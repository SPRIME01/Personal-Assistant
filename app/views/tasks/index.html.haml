%h1 Listing tasks

%table
  %tr
    %th Name
    %th Description
    %th Due by
    %th List
    %th Group
    %th Category
    %th Repeatable
    %th
    %th
    %th

  - @tasks.each do |task|
    %tr
      %td= task.name
      %td= task.description
      %td= task.due_by
      %td= task.list.name if task.list
      %td= task.group.name if task.group
      %td= task.category.name if task.category
      %td= is_repeatable(task)
      %td= link_to 'Show', task
      %td= link_to 'Edit', edit_task_path(task)
      %td= link_to 'Destroy', task, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Task', new_task_path
